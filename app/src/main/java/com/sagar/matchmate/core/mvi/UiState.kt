package com.sagar.matchmate.core.mvi

/**
 * Marker interface for UI State in MVI pattern.
 *
 * A State represents everything required by the UI
 * to render the current screen.
 */
interface UiState